

======================= TO START ======================= 
python -m uvicorn main:app --reload
in cd frontend: 
npm run dev
to refresh:
python sync_cache.py
========================================================

======================= ENV FILE =======================
Chempax_DSN="Chempax_Source"
Chempax_user="USERNAME HERE"
Chempax_pw="PASSWORD HERE"

pg_host="localhost"
pg_db="postgres"
pg_port="5432"
pg_user="postgres"
pg_password="password"
API_BASE_URL="http://localhost:8000/"
=========================================================


dont forget to save with f6 in pgAdmin 4 to save data changes

note to self: push to git and DO NOT run npm create again

hosted on http://localhost:5173/


in gitbash terminal : git push -u origin main
to push


AFTER commit or for start:
npm run dev (make sure in cd frontend)
python -m uvicorn main:app --reload

standard push:
git add .
git commit -m "message here"
git push

